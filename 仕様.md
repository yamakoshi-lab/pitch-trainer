# Pitch Trainer Pro - 仕様書 (仕様.md)

本ドキュメントは、音感を効果的に鍛え、聞いた音を現実の楽器（ピアノやギターなど）で即座に演奏できる能力（耳コピ・相対音感）を身につけるためのWebアプリケーション「Pitch Trainer Pro」のシステム仕様書です。

---

## 1. システム概要とアーキテクチャ

本システムは、サーバー管理不要かつ個人情報漏洩リスクをゼロにする「ゼロトラスト・サーバーレス構成」を採用しています。

### 1.1 全体構成
*   **フロントエンド:** Netlify（静的HTML / CSS / JavaScript / Chart.js）
*   **バックエンド / データベース:** Google Apps Script (GAS) ＋ Googleスプレッドシート
*   **インフラ連携:** 
    *   Netlifyの環境変数に `GAS_URL`（GASのウェブアプリURL）を登録。
    *   Netlifyのビルド時に環境変数をコードへ動的注入。
    *   インフラ更新時は、Netlify側で **「Deploy project without cache（キャッシュなし再デプロイ）」** を実行することで確実に反映。

---

## 2. 認証・ユーザー識別仕様（個人情報リスクゼロ設計）

メールアドレス等の個人識別情報（PII）の保持に伴う法的・運用的リスクを完全に排除するため、**「完全匿名ユーザー識別プロトコル」**を採用します。

### 2.1 2ちゃんねる風・短縮匿名ID生成アルゴリズム
1.  **生成タイミング:** ユーザーが初めてアプリを開いた瞬間。
2.  **生成ロジック:**
    *   文字空間: Base62（`0-9`, `a-z`, `A-Z` の62文字）。
    *   乱数ソース: `crypto.getRandomValues`（暗号論的疑似乱数）。
    *   長さ: 8文字。
    *   識別形式: 接頭辞 `ID:` を付与（例: `ID:x7K2mP9q`）。
3.  **永続化:** 生成したIDはブラウザの `localStorage` に保存（キー名: `pitch_app_anon_id`）。次回以降のアクセスでも同一IDを読み出し、過去データと整合。
4.  **衝突確率の数理的安全性 (誕生日のパラドックスによる検証):**
    *   IDの総パターン数: $62^8 \approx 2.18 \times 10^{14}$（約218兆通り）。
    *   ユニークユーザー数が10,000人規模であっても、誰か一人でもIDが衝突する確率は **0.00002% 以下**。宇宙の寿命が来るまで実質的に被らないため、個人情報を取得することなく安全に履歴管理が可能。

---

## 3. ユーザーインターフェース (UI/UX) 仕様

「かっこよさ（発光デザイン）」、「使いやすさ」、「親しみやすさ（キートップ表示）」、「現実の楽器への学習転移」を両立するアダプティブ・キーボードインターフェースを実装します。

### 3.1 デザインテーマ
*   **ビジュアル:** 漆黒（`#000000` / `#0f172a`）をベースとし、タップしたキーがネオンのように鮮やかに発光するサイバーパンク・ダークモード。
*   **アフォーダンス:** タップした感触、出来そう感を高めるために、キートップにはPCキーボード文字や音名をクリアに印字。

### 3.2 スマホ版UI (Mobile Safari 等向け)
*   **レイアウト:** 物理的なピアノ鍵盤（白鍵・黒鍵）を画面いっぱいに表示。
*   **目的:** 視覚的な音高の並び（x軸）を脳内にマッピングし、実物のピアノやキーボードへの学習転移を100%可能にする。

### 3.3 PC版UI
DAW（DTMソフト）で世界標準として採用されている「コンピュータキーボードによる演奏用マッピング」を実装。これにより、PCだけで高速な演奏トレーニングが可能になり、DTMスキルへも直接転移します。

*   **キーマッピング構造:**
    *   **白鍵（中段）:** `A`(C4/ド), `S`(D4/レ), `D`(E4/ミ), `F`(F4/ファ), `G`(G4/ソ), `H`(A4/ラ), `J`(B4/シ)...
    *   **黒鍵（上段）:** `W`(C#4), `E`(D#4), `T`(F#4), `Y`(G#4), `U`(A#4)...
    *   **オクターブ移動:** `Z`（1オクターブ下げる）、`X`（1オクターブ上げる）

---

## 4. 音響発振とiOS Safari（WebKit）対策

OS固有の制約を物理的にバイパスし、無遅延でクリアな音を再生します。

### 4.1 iOSのオーディオ遅延・スリープ対策 (Silent Audio Forcing)
iOS Safari等のWebKit環境では、省電力のためにオーディオハードウェアが頻繁にスリープ状態となり、発振時に最大300msのレイテンシが発生します。
*   **対策:** ページロード後の最初のユーザー操作（ログインまたはテスト開始タップ）時に、音量 `0.0001`、長さ `1ms` の無音に近い純音を裏で物理的に先行・継続発振させ、OSのオーディオスタックを常時「High-Performance Mode（覚醒状態）」に維持します。

### 4.2 音源生成モデル
*   Web Audio APIの `OscillatorNode` を用い、純粋なサイン波（`sine`）を生成。
*   周波数計算式（A4=440Hz基準）:
    $$f = 440 \times 2^{\frac{d - 69}{12}}$$
    (※ $d$ はMIDIノート番号)

---

## 5. ゲームルール・ロジック仕様

### 5.1 テスト構成
*   **全10問のセッション制:**
    *   **前半（Q1〜Q5）: 絶対音感テスト**
        *   基準音なし。ターゲットの音が単発で鳴り、その音高を当てる。
    *   **後半（Q6〜Q10）: 相対音感テスト**
        *   まず基準音（C4）が再生された後、ターゲットの音が鳴り、基準音との音程差を頼りに音高を当てる。
*   **制限時間:** 各問 3,000ms（3秒）の制限時間。残り時間に応じてタイマーバーが緑から赤へ変化。

### 5.2 スコア計算式
*   時間経過に伴い獲得スコアがリアルタイムで減衰（迅速な回答ほど高得点）。
*   **計算モデル:**
    $$\text{獲得スコア} = \max(0, 100 - (\text{半音の誤差} \times 15)) \times \max\left(0, 1 - \frac{\text{経過時間(ms)}}{3000}\right)$$
    *   完全一致（誤差0）かつ0秒回答で最大100点。
    *   タイムアウト（3000ms超過）時は 0点（誤差99として記録）。

---

## 6. 通信・セキュリティ仕様

### 6.1 バックエンド送信パケット（POST）
テスト終了後、以下の構造化された高解像度ログをGAS経由でスプレッドシートに記録します。
```json
{
  "userId": "ID:x7K2mP9q",
  "totalScore": 750,
  "details": [
    {
      "q": 1,
      "type": "absolute",
      "target": 60,
      "answered": 60,
      "distance": 0,
      "time_ms": 450
    },
    ...
  ]
}
```

### 6.2 セキュリティ対策
*   **PoW (Proof of Work) 検証:** 不正なスコア送信やDoS攻撃を防ぐため、フロントエンドでハッシュ演算（SHA-256）を行い、特定の難易度（例: 先頭に `'000'`）を満たす `nonce` を探索してから送信。GAS側で再検証。
*   **スプレッドシート・インジェクション対策:** 送信された文字列が `=, +, -, @` で始まる場合、GAS側で自動的に `'` を先頭に付与して数式評価を無効化（サニタイズ）。
*   **CORSポリシー:** Webアプリからの異なるOriginアクセスを許容するため、GAS側で `Access-Control-Allow-Origin: *` などのCORSヘッダーを返却。アクセス権限を「全員（Anyone）」に設定してデプロイ。
